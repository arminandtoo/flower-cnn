درباره پروژه
این مدل برای تشخیص 5 نوع مختلف گل بر اساس تصاویر آن‌ها آموزش دیده است. هدف این پروژه، نمایش یک پیاده‌سازی پایه و استاندارد از CNN برای وظایف طبقه‌بندی تصویر (Image Classification) است.
معماری مدل
مدل استفاده شده یک tf.keras.Sequential ساده با ساختار زیر است:

لایه Rescaling (نرمال‌سازی ورودی)

لایه Conv2D (32 فیلتر، کرنل 3x3، فعال‌ساز 'relu')

لایه MaxPooling2D

لایه Conv2D (32 فیلتر، کرنل 3x3، فعال‌ساز 'relu')

لایه MaxPooling2D

لایه Conv2D (32 فیلتر، کرنل 3x3، فعال‌ساز 'relu')

لایه MaxPooling2D

لایه Flatten (تبدیل داده‌های چندبعدی به بردار)

لایه Dense (128 نورون، فعال‌ساز 'relu')

لایه Dense (5 نورون - لایه خروجی برای 5 کلاس)
